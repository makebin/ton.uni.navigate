### 基于uniapp的 页面跳转lib

```ts
import uniNavigate from 'ton.uni.navigate';
interface ipages = {
  path:"",
  name:"",
  meta:{
    target:""
  }
}
const handle = new uniNavigate(ipages[]:pages);

handle.to(string|ipages:page);

```
